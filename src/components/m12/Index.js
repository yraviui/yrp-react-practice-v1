import React from 'react'

const M12 = () => {
/*     const [serviceData, setServiceData] = useState([])
    const getAllTodos = async () => {
      getTodos().then( res => setServiceData(res.data)).catch( err => console.log(err))
    }
    useEffect(() => {
        getAllTodos()
    }) */
  return (
    <div>
      <h1>M12 - Service</h1>
      {/* <ul>
        {serviceData.map((o, i) => <li key={i}>{o.title}</li>)}
      </ul> */}
    </div>
  )
}

export default M12
